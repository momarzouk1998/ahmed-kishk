'use client';

// #FIX: كان الملف ده بيشغّل مؤقّت خلفى كل 15 ثانية على كل صفحة، وبيعمل حاجة خطيرة —
// لو السيرفر رجع فاضى لأي مفتاح (مثلاً وقت إعادة نشر أو تعارض شبكة مؤقت)، كان بيدفع
// النسخة القديمة المخزّنة على قرص الجهاز (localStorage) لفوق تانى للسيرفر معتبرها
// "بذرة" — يعني ممكن يرجّع بيانات قديمة/غلط فوق بيانات حقيقية جديدة بدون أي تنبيه.
// النظام دلوقتى مفيش فيه أى تخزين محلى على القرص إطلاقًا؛ كل صفحة بتجيب بياناتها
// مباشرة من قاعدة البيانات عن طريق fetch حقيقى، فمفيش داعى للمزامنة الخلفية دي خالص.
// الدالتين تحت اتسابوا بس عشان الصفحات القديمة اللي لسه بتستدعيهم متتعطلش.

export function onSyncReady(cb: () => void): void {
  cb();
}

export function initCentralSync(): void {
  // لا يوجد أى تخزين محلى للمزامنة معه بعد اليوم.
}
